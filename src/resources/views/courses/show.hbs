<div class="mt-4">
    <div class="row">
        <div class="col-sm-3">
            <button>Student now</button>
        </div>
        <div class="col-sm-9">
            <h1>{{courses.name}}</h1>
            <img src="{{courses.image}}" alt="image details" width="300" height="300">
            <p>{{courses.description}}</p>
        </div>
    </div>
</div>